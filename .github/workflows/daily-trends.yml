# ================================================================
# 每日文学热点 + 个性化书籍推荐
# 每天北京时间 00:00（UTC 16:00）执行
# ================================================================
# 需要的 GitHub Secrets：
#   ANTHROPIC_API_KEY  — 用于 Claude API 深度分析及推荐理由生成（必填）
#   OPENAI_API_KEY     — 降级备选，Anthropic 不可用时使用（选填）
#   SERPER_API_KEY     — Google 搜索 API，用于扩展新闻源（选填）
# ================================================================

name: Daily Literature Trends

on:
  schedule:
    # 北京时间每天零点 (UTC+8 → UTC 16:00)
    - cron: "0 16 * * *"
  workflow_dispatch:

jobs:
  fetch-and-commit:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build works snapshot
        run: npm run recommend:snapshot

      - name: Fetch daily trends
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          SERPER_API_KEY: ${{ secrets.SERPER_API_KEY }}
        run: npm run trends:fetch

      - name: Fetch daily recommendation
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: npm run recommend:fetch

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          CHANGED=0

          if ! git diff --quiet HEAD -- data/daily-trends.json; then
            git add data/daily-trends.json
            CHANGED=1
          fi

          if ! git diff --quiet HEAD -- data/daily-recommendation.json; then
            git add data/daily-recommendation.json
            CHANGED=1
          fi

          if ! git diff --quiet HEAD -- data/recommendation-history.json; then
            git add data/recommendation-history.json
            CHANGED=1
          fi

          if ! git diff --quiet HEAD -- data/works-snapshot.json; then
            git add data/works-snapshot.json
            CHANGED=1
          fi

          if [ "$CHANGED" -eq 1 ]; then
            git commit -m "data: 更新每日文学热点与书籍推荐 [automated]"
            git push
            echo "✅ 已推送更新，Vercel 将自动重新部署"
          else
            echo "⏭ 所有数据文件无变化，跳过 commit"
          fi
